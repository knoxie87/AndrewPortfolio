const Overlay = (props) => {
    return (
        <div style={{width: '100%',height: '100%', position:'fixed', top: 0, left: 0, backgroundColor:'rgb(0,0,0,0.25)', zIndex:-20}}>

        </div>
    )
}

export default Overlay